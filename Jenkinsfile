pipeline {
    agent {dockerfile { filename 'DOTT/cidr_convert_api/python/Dockerfile.build' }}
    stages {
        stage('Build') { 
            steps {
                echo "HOaLA"
            }
        }
    }
}

